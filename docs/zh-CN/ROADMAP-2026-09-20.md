# Supplemental Loop Engineering 路线图 — 2026-09-20

[English](../ROADMAP-2026-09-20.md) | **简体中文**

已接受的 Stage 1-12 / Project Complete 历史继续有效。后续新增需求只开启 supplemental chain。

## Stage 13 — SUPPLEMENTAL-SOT-DEPLOYMENT-FOUNDATION
Owner：把新的部署/产品契约 canonicalize，并完成 fresh-machine prerequisite/provision foundation。

输出：
- supplemental product goals；
- deployment contract；
- concurrency/fallback contract；
- 更新 SoT/architecture/risks；
- Tailscale environment gate；
- approved WebGPT runtime binary provisioning；
- 不修改健康的现有 MCP。

## Stage 14 — PRODUCTION-UNIFIED-GATEWAY-EDGE
Owner：把 Stage5 风格的 Edge evidence 提升为 production runtime contract。

输出：production OAuth secret lifecycle、WebGPT Edge Runtime Supervisor ownership、Gateway upstream route sync、Tailscale Funnel、local/public URL status、无需 backend-specific public tunnel、restart-safe/idempotent edge。

## Stage 15 — COMPONENT-INSTALL-UPGRADE-LIFECYCLE
Owner：单仓库 discovery/install/upgrade adapter。

输出：component install metadata/schema、latest-stable/version detection、preserve/install/upgrade/diagnose 状态机、Node/uv/system prerequisite、所选 MCP install adapter、仅对 WebGPT-installed instance 的 lifecycle authority、duplicate prevention。

## Stage 16 — CONCURRENCY-SESSION-ISOLATION-FALLBACK
Status: `CLOSED_LOCAL_VERIFIED`。

Owner：多窗口并发与互补 rescue path。

输出：Serena fixed-project pool、Coding Tools workspace/write boundary、Remote Desktop Commander/Windows-MCP GUI lease、Gateway ↔ Remote Desktop Commander recovery routing、loop prevention、exact side-effect ownership、concurrency tests。

## Stage 17 — MANAGER-UX-BILINGUAL-DESKTOP
Status: `CLOSED_LOCAL_VERIFIED`。

输出：中英文 Manager 功能等价、当前部署桌面默认中文、环境/部署/版本/Gateway/OAuth/HTTPS/inventory surfaces、MCP URL copy/open、OAuth set/reveal/regenerate、migration candidate add/remove、真实 action feedback/activity、功能状态驱动 motion、accessibility/reduced-motion。

Post-acceptance Hotfix: `CLOSED_LOCAL_VERIFIED`：
- stale owned/legacy Manager 只有 identity + contract proof 后才刷新；
- ambiguous/unrelated 9200 listener fail closed；
- desktop launcher 复用正常 Edge profile，不创建 automation-only profile；
- two-pass real Desktop launch 保留 core MCP/Gateway/Manager PIDs。

## Stage 18 — CHINESE-MIRROR-AND-THIRD-PARTY-NOTICES
Owner：完整中文镜像与公开 dependency provenance。
Status: `CLOSED_LOCAL_VERIFIED`。

输出：
- 全 tracked human-readable candidate audit；
- 当前人类可读内容完整中文处理；
- `LICENSE` 原始 legal text 保持不变 + 非约束中文阅读译本；
- translation coverage manifest；
- README 之外的 dedicated third-party notices/provenance；
- package/release 中双语公开资源。

收口证据：155 个候选显式分类；原始 `LICENSE` 字节/hash 保持并提供
非约束中文阅读译本；8 component + 4 Python dependency provenance；
targeted 27 PASS；full 191 PASS；Ruff/secret/diff PASS；isolated installed
wheel 验证双语 Manager 与 9 个 Stage18 release/legal/provenance resource。

## Stage 19 — END-TO-END-DEPLOYMENT-ACCEPTANCE
Owner：证明完整部署契约。

输出：clean/fresh-state simulation、existing-machine preserve、duplicate prevention、version/install adapter tests、Gateway safe calls、OAuth DCR/PKCE/refresh/401/authenticated MCP、Tailscale HTTPS、可用时的 ChatGPT browser OAuth acceptance、fallback drills、multi-window concurrency、wheel/install/Desktop Manager、full gates。

## SUPPLEMENTAL-FINAL-ACCEPTANCE
Owner：把原已接受项目与 supplemental chain 对齐，确保没有未解决 security/deployment/concurrency contradiction。

## Stage sizing
继续使用包含 closure/handoff 的约 20 分钟 soft budget。它不是平台 timeout；必要时只在当前 owner concern 内拆分，不能重开已关闭 Stage。

CURRENT_STAGE = STAGE-19-END-TO-END-DEPLOYMENT-ACCEPTANCE
NEXT_STAGE = SUPPLEMENTAL-FINAL-ACCEPTANCE
AFTER_NEXT_STAGE = GLOBAL_LOOP_COMPLETE
