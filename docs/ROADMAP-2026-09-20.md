# Supplemental Loop Engineering Roadmap — 2026-09-20

[**English**](ROADMAP-2026-09-20.md) | [简体中文](zh-CN/ROADMAP-2026-09-20.md)

The previously accepted Stage 1-12 / Project Complete history remains valid. New user requirements reopen only a supplemental chain.

## Stage sequence

### STAGE-13-SUPPLEMENTAL-SOT-DEPLOYMENT-FOUNDATION
Owner: canonicalize the new deployment/product contract and complete fresh-machine prerequisite/provision foundations.

Required outputs:
- supplemental product goals;
- deployment contract;
- concurrency/fallback contract;
- updated SoT/architecture/risks;
- Tailscale environment gate;
- approved WebGPT runtime binary provisioning;
- no mutation of healthy existing MCPs.

### STAGE-14-PRODUCTION-UNIFIED-GATEWAY-EDGE
Owner: promote Stage 5-style Edge evidence into a production runtime contract.

Required outputs:
- production OAuth secret lifecycle;
- Runtime Supervisor ownership for WebGPT Edge only;
- Gateway upstream route sync;
- Tailscale Funnel publication;
- local/public URL status;
- no requirement for backend MCP-specific public tunnels;
- restart-safe/idempotent edge behavior.

### STAGE-15-COMPONENT-INSTALL-UPGRADE-LIFECYCLE
Owner: one-repository discovery/install/upgrade adapters.

Required outputs:
- component install metadata/schema;
- latest-stable/version detection;
- preserve/upgrade/install/diagnose state machine;
- Node/uv/system dependency detection;
- installation adapters for the selected MCP set;
- lifecycle authority only for WebGPT-installed instances;
- duplicate-deployment prevention.

### STAGE-16-CONCURRENCY-SESSION-ISOLATION-FALLBACK
Owner: multi-window concurrency plus complementary rescue paths.

Status: CLOSED_LOCAL_VERIFIED.

Required outputs:
- Serena fixed-project instance pool/project-slot design and implementation;
- Coding Tools workspace concurrency policy/pool boundary;
- machine GUI lease policy for Remote Desktop Commander/Windows-MCP;
- Gateway <-> Remote Desktop Commander recovery routing;
- recovery loop prevention and exact side-effect ownership;
- concurrency tests.

### STAGE-17-MANAGER-UX-BILINGUAL-DESKTOP
Owner: user-facing one-stop Manager and desktop experience.
Status: CLOSED_LOCAL_VERIFIED.

Required outputs:
- optimized English + Chinese Manager with functional parity;
- default Chinese desktop launcher for the current deployment;
- environment/deployment/version/Gateway/OAuth/HTTPS/inventory surfaces;
- MCP URL copy/open controls;
- local OAuth password set/reveal/regenerate flow;
- add/remove migration-candidate flows;
- real action feedback and activity log;
- requested motion system tied to functional state;
- accessibility/reduced-motion behavior.

Post-acceptance Hotfix: CLOSED_LOCAL_VERIFIED.
- stale owned/legacy WebGPT Manager generations are refreshed only after identity + contract proof;
- ambiguous/unrelated 9200 listeners remain fail-closed;
- desktop launcher reuses the user's normal Edge profile instead of creating an automation-only profile;
- two-pass real Desktop launch preserved all core MCP/Gateway PIDs and rendered the complete current Manager.

### STAGE-18-CHINESE-MIRROR-AND-THIRD-PARTY-NOTICES
Owner: complete the Chinese mirror and public dependency provenance.
Status: CLOSED_LOCAL_VERIFIED.

Required outputs:
- all tracked English files audited;
- complete Chinese mirror treatment for human-readable content;
- legal LICENSE original preserved plus Chinese reading translation;
- translation coverage manifest;
- dedicated third-party notices/provenance outside README;
- package/release resources updated for both language experiences.

Closure evidence: 155 classified text-format candidates/dispositions; legal
`LICENSE` byte/hash preservation plus non-binding Chinese reading translation;
8 component + 4 Python dependency provenance reconciliation; 27 targeted PASS;
191 full PASS; Ruff/secret/diff gates PASS; isolated installed wheel verified
the bilingual Manager and 9-file Stage18 release/legal/provenance resource set.

### STAGE-19-END-TO-END-DEPLOYMENT-ACCEPTANCE
Owner: prove the complete deployment contract.

Required outputs:
- clean/fresh-state deployment simulation;
- existing-machine preserve test;
- duplicate prevention;
- latest-version/install adapter tests;
- Gateway routing and safe calls;
- OAuth DCR/PKCE/refresh/401/authenticated MCP;
- Tailscale HTTPS;
- ChatGPT browser OAuth acceptance when the interactive surface is available;
- complementary fallback drills;
- multi-window concurrency tests;
- wheel/install/desktop Manager validation;
- full repository gates.

### SUPPLEMENTAL-FINAL-ACCEPTANCE
Owner: reconcile the original accepted project with the supplemental chain, with no unresolved security/deployment/concurrency contradiction.

## Stage sizing

Keep the existing approximately 20-minute soft budget with explicit closure reserve. Split only within the current owner concern if necessary; do not reopen earlier accepted stages.

## Current pointers

CURRENT_STAGE = STAGE-19-END-TO-END-DEPLOYMENT-ACCEPTANCE
NEXT_STAGE = SUPPLEMENTAL-FINAL-ACCEPTANCE
AFTER_NEXT_STAGE = GLOBAL_LOOP_COMPLETE
